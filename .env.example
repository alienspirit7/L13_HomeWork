# Weather Data Agent - Environment Variables Template
# Copy this file to .env and fill in your actual values

# LLM Provider Selection
# Choose 'gemini' (default, free tier available) or 'anthropic'
LLM_PROVIDER=gemini

# Gemini API Configuration (required if LLM_PROVIDER=gemini)
# Get your API key from: https://aistudio.google.com/app/apikey
# Option 1: Point to a file containing your key (recommended)
GEMINI_API_KEY=keys/gemini_api_key
# Option 2: Put the key directly here (less secure)
# GEMINI_API_KEY=your_gemini_api_key_here

# Anthropic API Configuration (required if LLM_PROVIDER=anthropic)
# Get your API key from: https://console.anthropic.com/
# Option 1: Point to a file containing your key (recommended)
ANTHROPIC_API_KEY=keys/anthropic_api_key
# Option 2: Put the key directly here (less secure)
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Google Cloud Configuration
# Option 1: Service Account Key (recommended)
# Create a service account in GCP Console with BigQuery Data Viewer role
# Download the JSON key and provide the full path here
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# Option 2: Application Default Credentials
# If you leave GOOGLE_APPLICATION_CREDENTIALS empty, the system will attempt
# to use Application Default Credentials. Set this up with:
# gcloud auth application-default login
